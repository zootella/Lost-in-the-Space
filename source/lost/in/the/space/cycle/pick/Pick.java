package lost.in.the.space.cycle.pick;

import java.util.ArrayList;
import java.util.Collections;
import java.util.List;

import lost.in.the.space.cycle.have.HaveFile;

public class Pick {
	
	
	// me algorithm
	
	// remove hashes we already have
	// remove extensions that don't match
	// point for middle sources
	// point for middle size
	// point for middle name length
	// randomly select 3 from the winners
	
	public static List<PickFile> pick(List<HaveFile> have, List<PickFile> results, String keyword, String ext) {
		
		
		
//		Collections.sort(results, new ByName(null));
		
		
		
		List<PickFile> pick = new ArrayList<PickFile>();
		return pick;
		
	}

}
